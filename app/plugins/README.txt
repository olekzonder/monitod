# Пример плагина с объяснениями
# Название файла -- {название плагина}.py
Название плагина должно быть ясным и не иметь пробелов

from monitor import Monitor # Внутри базового класса реализован метод для чтения данных
class Plugin(Monitor): # название плагина должно быть Plugin, и только Plugin
    def get_data(self): # В этом методе необходимо реализовать логику обработки данных
    # Метод read поддерживает следующие параметры:
    # rootfs -- чтение файлов из procfs или sysfs
        info = self.read('rootfs','/proc/cpuinfo')
    # logs -- чтение прочих файлов из логов
        # не реализовано
    # systemd -- получение логов systemd
        # не реализовано
    # command -- получение результатов работы команды
        # не реализовано
    # process -- получение сведений о процессах
        # не реализовано
        for item in info: # Полученные данные разбиваются на группы (в случае с /proc/cpuinfo -- ядра, в случае с sysfs -- каждый каталог)
            self.data[key] = item[key] # Можно добавить значение
            self.data.append(key) # Можно добавить группу значений, например, частоты у одного процесса, или зону температурных датчиков
            self.data[key][key2] = item[key2] 
        return self.data # В конце вывод сформированных данных

